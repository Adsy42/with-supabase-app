import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable cacheComponents to allow dynamic routes with client components
  // cacheComponents: true,
};

export default nextConfig;
